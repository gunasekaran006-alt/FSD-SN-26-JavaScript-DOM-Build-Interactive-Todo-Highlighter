# Simple Todo Highlighter

A basic web app to learn how JavaScript interacts with HTML using DOM selectors.

### Features:
- Change title text using `getElementById`.
- Access task list using `getElementsByClassName`.
- Select buttons using `querySelector`.
- Highlight multiple items using `querySelectorAll`.

### How to use:
Just open `index.html` in your browser and click the button to see the changes!